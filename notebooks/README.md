# Notebooks

This directory is for original explanatory analyses and visualizations. Public
notebooks must be restartable from approved inputs and must not contain private
follow-along work, secrets, raw datasets, or large embedded outputs.

## Week 3

`week_03/week_03_mlp_protein_context.ipynb` is an original public notebook
with no committed outputs. It reads only `reports/week_03/mlp_evaluation_v1.json`
after the operator publishes that aggregate report. It does not train, load a
collection, inspect a checkpoint, or access sealed data.
